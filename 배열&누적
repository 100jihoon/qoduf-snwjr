#include <iostream>

int main() 
{
  int score[4];
  int sum = 0;

  printf("프로그래밍 점수 입력 :");
  scanf("%d", &score[0]);
  printf("전기회로 점수 입력 :");
  scanf("%d", &score[1]);

  for(int i = 0; i<=1; i++)
    {
      sum = sum+score[i];
    }
  printf("\n 점수 합계 = %d", sum);
  return 0;
  }
